const Base = 0.4

/* eslint-disable @typescript-eslint/no-magic-numbers */
export const Spaces = {
  SPACE04: Base,
  SPACE08: Base * 2,
  SPACE12: Base * 3,
  SPACE16: Base * 4,
  SPACE20: Base * 5,
} as const
