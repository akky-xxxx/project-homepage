POSTGRES_URL=postgres://postgres:postgres@127.0.0.1:5432/cms

# secret は 32 文字以上が必須。`openssl rand -base64 32` で生成する
PAYLOAD_SECRET=YOUR_SECRET_HERE
BETTER_AUTH_SECRET=YOUR_BETTER_AUTH_SECRET_HERE

# passkey の rpID になるため、本番では固定のカスタムドメインを指定する
BETTER_AUTH_URL=http://localhost:3000

# サインアップを許可する唯一のメールアドレス。未設定ならサインアップは常に拒否される。
# 初期管理者の登録が済んだらこの環境変数を削除して再デプロイする。
# ローカルでは int テストのテストユーザーに合わせる
SIGN_UP_ALLOWED_EMAIL=dev@payloadcms.com

# 画像の保存先。未設定ならローカルディスクに保存する(ローカル開発・CI 用)
# BLOB_READ_WRITE_TOKEN=

# "true" で passkey 登録済みでも password ログインできるようになる(localhost のみ有効)。
# 本番 DB へローカルから繋いで写真を投入するときだけコマンドラインで渡す。
# .env にも Vercel にも設定しないこと(README「写真の投入」参照)
# PASSWORD_SIGN_IN_ENABLED=true
